# Where this color data comes from

Generated by `scripts/bake-cards.mjs` on **2026-08-29** from the public MCP
endpoint of a real art supply store in Santiago, Chile
(`https://www.boykot.cl/api/mcp`, tool `get_color_card`, no credentials required).

**1677 tones across 17 color cards, 1625 of them with a
verified hex value.**

Hex values are baked into the repository because they never change. Stock is
*not* baked — it is fetched live at runtime, because a frozen stock number
would be a lie. No internal inventory identifiers are stored here.

## Coverage, honestly

- `copic-sketch` — 336/358 tones with hex (94%) · alcohol-marker
- `copic-ink` — 336/358 tones with hex (94%) · alcohol-ink-refill
- `copic-ciao` — 174/179 tones with hex (97%) · alcohol-marker
- `copic-classic` — 211/213 tones with hex (99%) · alcohol-marker
- `copic-wide` — 35/36 tones with hex (97%) · alcohol-marker
- `molotow-premium` — 224/224 tones with hex (100%) · solvent-spray
- `molotow-premium-plus` — 12/12 tones with hex (100%) · solvent-spray
- `molotow-premium-neon` — 8/8 tones with hex (100%) · solvent-spray
- `molotow-one4all-2mm` — 50/50 tones with hex (100%) · acrylic-marker
- `molotow-one4all-4mm` — 50/50 tones with hex (100%) · acrylic-marker
- `molotow-one4all-1.5mm` — 40/40 tones with hex (100%) · acrylic-marker
- `molotow-one4all-acrylic-twin` — 48/48 tones with hex (100%) · acrylic-marker
- `uni-posca-3m` — 33/33 tones with hex (100%) · water-based-paint-marker
- `uni-posca-5m` — 27/27 tones with hex (100%) · water-based-paint-marker
- `uni-posca-8k` — 26/26 tones with hex (100%) · water-based-paint-marker
- `uni-posca-17k` — 8/8 tones with hex (100%) · water-based-paint-marker
- `angelus-pearlescents-1oz` — 7/7 tones with hex (100%) · leather-acrylic

### Tones without a hex value

52 tones ship with `hex: null`. They are **never painted and never
recommended as a color match** — the interface draws them with a diagonal
hatch and the label `unmapped`, and the tools report them as unmapped rather
than guessing. A color the manufacturer never made is worse than no answer.

### Cards the store sells that are *not* here

The store's catalog has 62 color cards, but only 17 carry per-tone hex data.
The other 45 — including the whole Holbein watercolor range, Createx airbrush,
ZIG and most of the Angelus leather line — are sold with no color values
mapped, so they cannot be painted or matched, and are deliberately absent
rather than approximated.

**Copic ACREA** deserves a specific mention: the store sells it (6 SKUs, a
water-based *opaque* acrylic marker that behaves nothing like the alcohol
markers above), but it has no color card at all, so it is invisible to this
workbench. That is a gap in the source data, not a choice.

## Reproducing

```bash
node scripts/bake-cards.mjs
```

The endpoint is public and unauthenticated. If this script ever needs a
credential, something has been designed wrong.
